apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - rbac.yaml
  - job.yaml
configMapGenerator:
  - name: spcg-oauth-bootstrap
    files:
      - bootstrap.sh
    options:
      disableNameSuffixHash: true
