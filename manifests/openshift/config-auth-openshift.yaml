# Auth modes for OpenShift: primary OAuth + optional kubeconfig upload (break-glass).
apiVersion: v1
kind: ConfigMap
metadata:
  name: spcg-auth-env
  namespace: pcap-frontend
data:
  SPCG_AUTH_METHODS: openshift,kubeconfig
