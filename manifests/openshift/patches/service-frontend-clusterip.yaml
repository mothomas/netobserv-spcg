# OpenShift Routes terminate TLS; frontend Service stays ClusterIP (no NodePort).
apiVersion: v1
kind: Service
metadata:
  name: spcg-frontend
  namespace: pcap-frontend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 3000
