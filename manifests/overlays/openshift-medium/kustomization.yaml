# OpenShift Medium tier — HA UI, S3 required, higher admission caps.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../openshift-small
patches:
  - path: patch-admission.yaml
  - path: patch-replicas.yaml
  - path: patch-ui-portal-resources.yaml
